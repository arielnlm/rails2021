<p id="notice"><%= notice %></p>

<h1>
  <%= @event.name %>
</h1>

<h3>
   <strong>Info:</strong>
  <%= @event.description %>
</h3>

<h3>
 <strong> Bookstore:</strong>
  <%= @event.bookstore.name %>
</h3>

<h3>
  <strong>Location:</strong>
  <%= Location.find(@event.bookstore.location_id).street %> <%= Location.find(@event.bookstore.location_id).city %> <%= Location.find(@event.bookstore.location_id).country %>
</h3>

<% if is_admin? %>
  <%= link_to 'Edit', edit_event_path(@event) %> |
  <%= link_to 'Back', events_path %>
<% end %>

