<%= form_with(model: bookstore) do |form| %>
  <% if bookstore.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bookstore.errors.count, "error") %> prohibited this bookstore from being saved:</h2>

      <ul>
        <% bookstore.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  
  <div class="field">
    <%= form.label :location_id %>
    <%= form.number_field :location_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
